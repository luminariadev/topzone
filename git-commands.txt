================================
 TOPZONE - 10 GIT COMMANDS
================================
Jalankan satu per satu di terminal
================================

cd "D:\Rizkia\Project Software\TopZone"

git add src/types/index.ts && git commit -m "feat(types): add TypeScript type definitions for core data models"

git add src/utils/format.ts src/utils/helpers.ts && git commit -m "feat(utils): add format and helper utility functions"

git add src/layouts/Layout.astro && git commit -m "feat(seo): add SEO meta tags and Open Graph to layout"

git add vitest.config.ts src/utils/__tests__/format.test.ts src/utils/__tests__/helpers.test.ts && git commit -m "feat(testing): add Vitest setup with unit tests"

git add src/components/Toast.astro && git commit -m "feat(ui): add toast notification component"

git add src/components/Breadcrumb.astro && git commit -m "feat(ui): add breadcrumb navigation component"

git add src/stores/wishlist.ts src/components/WishlistButton.astro && git commit -m "feat(wishlist): add wishlist feature with nanostore persistence"

git add src/utils/filters.ts && git commit -m "feat(utils): add product search and filter utilities"

git add src/components/ScrollToTop.astro && git commit -m "feat(ui): add animated scroll-to-top button"

git add package.json package-lock.json && git commit -m "chore(deps): add vitest as dev dependency"

git log --oneline -12



================================
 OPSI: 1 COMMIT SAJA
================================

cd "D:\Rizkia\Project Software\TopZone"
git add -A
git commit -m "feat: add types, utils, SEO, testing, toast, breadcrumb, wishlist, filters, scroll-to-top"
git push origin develop
